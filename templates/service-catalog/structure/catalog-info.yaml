apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.name }}
  description: Recommended path service scaffold for ${{ values.name }}
  tags:
    - ${{ values.runtime }}
    - recommended-path
spec:
  type: service
  lifecycle: experimental
  owner: ${{ values.owner }}
