apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ${{ values.kubeNamespace }}
resources:
  - ../../base
images:
  - name: ${{ values.imageRegistry }}/${{ values.name }}
    newTag: initial
